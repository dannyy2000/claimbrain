{
  "name": "claimbrain",
  "version": "1.0.0",
  "description": "Autonomous insurance settlement protocol on Somnia Agentic L1",
  "private": true,
  "workspaces": [
    "contracts",
    "agent",
    "frontend"
  ],
  "scripts": {
    "install:all": "npm install && npm install --prefix contracts && npm install --prefix agent && npm install --prefix frontend",
    "deploy": "npm run deploy --prefix contracts",
    "agent": "npm run start --prefix agent",
    "dev": "npm run dev --prefix frontend"
  },
  "keywords": [
    "somnia",
    "insurance",
    "ai-agent",
    "blockchain",
    "defi",
    "autonomous"
  ],
  "author": "Daniel Akinsanya",
  "license": "MIT"
}
